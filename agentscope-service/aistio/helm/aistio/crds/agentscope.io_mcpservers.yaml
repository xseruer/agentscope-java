---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: mcpservers.agentscope.io
spec:
  group: agentscope.io
  names:
    kind: MCPServer
    listKind: MCPServerList
    plural: mcpservers
    shortNames:
    - mcp
    singular: mcpserver
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .spec.remote.url
      name: URL
      priority: 1
      type: string
    - jsonPath: .status.discoveredTools
      name: Tools
      priority: 1
      type: integer
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MCPServer defines an MCP service registration.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: MCPServerSpec defines the desired state of MCPServer.
            properties:
              allowedNamespaces:
                description: AllowedNamespaces defines namespace access policy.
                properties:
                  from:
                    type: string
                type: object
              description:
                maxLength: 512
                type: string
              remote:
                description: Remote transport configuration. Required when type=Remote.
                properties:
                  headersFrom:
                    items:
                      description: HeaderFromRef references a header value from a
                        Secret.
                      properties:
                        header:
                          type: string
                        key:
                          type: string
                        kind:
                          type: string
                        name:
                          type: string
                      required:
                      - header
                      - key
                      - kind
                      - name
                      type: object
                    type: array
                  protocol:
                    default: STREAMABLE_HTTP
                    enum:
                    - STREAMABLE_HTTP
                    - SSE
                    type: string
                  timeout:
                    default: 30s
                    type: string
                  url:
                    pattern: ^https?://
                    type: string
                required:
                - url
                type: object
              stdio:
                description: Stdio transport configuration. Required when type=Stdio.
                properties:
                  args:
                    items:
                      type: string
                    type: array
                  command:
                    minLength: 1
                    type: string
                  env:
                    additionalProperties:
                      type: string
                    type: object
                required:
                - command
                type: object
              type:
                description: MCPServerType defines the transport type.
                enum:
                - Remote
                - Stdio
                type: string
            required:
            - type
            type: object
          status:
            description: MCPServerStatus defines the observed state of MCPServer.
            properties:
              conditions:
                items:
                  description: Condition represents a status condition.
                  properties:
                    lastTransitionTime:
                      format: date-time
                      type: string
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      description: ConditionType defines the type of condition.
                      type: string
                  required:
                  - status
                  - type
                  type: object
                type: array
              discoveredTools:
                items:
                  description: DiscoveredTool represents a tool discovered from the
                    MCP server.
                  properties:
                    description:
                      type: string
                    name:
                      type: string
                  required:
                  - name
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
